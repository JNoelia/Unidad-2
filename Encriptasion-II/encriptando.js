
function encriptar() {
    var passwordsistema = "1234";
    var mensajeError = "Contraseña erronea";
    var mensaje = document.getElementById("mensaje").value;
    var password = document.getElementById("password").value;

    // alert(mensaje);

    if (password == passwordsistema) {
        // alert("Tu contraseña es correcta");

        document.getElementById("error").innerHTML = "";

        var mensaje_encriptado = btoa(mensaje);
        document.getElementById("msjencrip").innerHTML = mensaje_encriptado;



    } else {
        // alert(mensajeError);
        document.getElementById("error").innerHTML = mensajeError;
    }
}

 function desencriptar() {
     var msjCodificado = document.getElementById("msCodigo").value;
     var msjDecodificado = atob(msjCodificado)
     // alert(mensajeDesencriptado)
     document.getElementById("msjReal").innerHTML = msjDecodificado;
    }
 



































































































































































































































































































































































































































































































































































































































































































































































































































